# Beam Value Flow

Standalone pipeline for EDA23 using:

- offline rollout collection
- state-action value regression
- beam search guided by the value model
- real `imap` evaluation to correct model ranking

This directory is intentionally independent from `rl_flow/`.
